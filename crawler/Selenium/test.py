from selenium import webdriver
from selenium.webdriver.common.keys import Keys
import time

wd = webdriver.Chrom() #같은 폴더에 있으므로 생략, 만약 다른 디렉터리에 있다면 명시해줘야함
driver.get("https://www.naver.com/") #네이버에 접속
